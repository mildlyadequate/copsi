{
  "name": "copsi-client",
  "productName": "Copsi",
  "version": "0.2.0",
  "description": "Collaboration App",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/appicon/mac/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . copsi-client --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/appicon/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CopsiApp --version-string.FileDescription=CE --version-string.ProductName=\"Copsi\""
  },
  "author": "COPSI",
  "license": "MIT",
  "dependencies": {
    "electron-json-storage": "^4.1.6",
    "jquery": "^3.3.1",
    "moment": "^2.24.0",
    "read-multiple-files": "^3.0.0",
    "shortid": "^2.2.14",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "devtron": "^1.4.0",
    "electron": "^3.1.4",
    "electron-packager": "^13.0.1"
  }
}
